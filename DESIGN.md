# 太极 Code 客户端界面设计规范

> 复刻 Claude Code 终端交互风格，融合太极美学

---

## 1. 设计哲学

### 1.1 核心原则

- **极简主义** — 去除冗余装饰，信息密度适中
- **功能可见性** — 每个视觉元素都有明确的功能含义
- **渐进披露** — 复杂信息默认折叠，按需展开
- **即时反馈** — 所有操作都有视觉响应

### 1.2 视觉语言

```
┌─────────────────────────────────────────────────────────────┐
│  太极 Code 界面 = 清晰的层次 + 克制的色彩 + 流畅的动效        │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 整体布局

### 2.1 屏幕分区

```
┌─────────────────────────────────────────────────────────────┐
│  [头部] 太极 Code                                    v0.3.0  │  ← 1行
─────────────────────────────────────────────────────────────┤
│                                                             │
│  [内容区]                                                    │
│  • 用户消息                                                  │
│  • 助手响应                                                  │
│  • 工具调用/结果                                              │
│  • 系统通知                                                  │
│                                                             │
│  (可滚动区域)                                                 │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [状态栏] 对话 3 轮    [规划]    Shift+Tab 切换模式  /help   │  ← 1行
├─────────────────────────────────────────────────────────────┤
│  [输入区] > 你好，请帮我检查这个项目的代码质量                  │  ← 1-3行
└─────────────────────────────────────────────────────────────┘
```

### 2.2 尺寸规范

| 区域 | 高度 | 说明 |
|------|------|------|
| 头部 | 1行 | 应用名称 + 版本 |
| 内容区 | 动态 | 总高度 - 5行 |
| 状态栏 | 1行 | 模式、轮次、快捷键提示 |
| 分隔线 | 1行 | ────────────────── |
| 输入区 | 1-3行 | 支持多行输入 |

---

## 3. 配色方案

### 3.1 主色调

```
┌─────────────────────────────────────────────────────────────┐
│  颜色名称        ANSI    色值        用途                     │
├─────────────────────────────────────────────────────────────┤
│  太极橙          204     #E06050    助手消息、强调元素        │
│  用户蓝          75      #5FAFFF    用户消息、输入提示符      │
│  工具灰          243     #767676    工具调用名称              │
│  结果灰          240     #585858    工具结果内容              │
│  错误红          196     #FF0000    错误信息                  │
│  成功绿          78      #5FD7AF    成功状态、自动模式        │
│  警告黄          214     #FFAF00    权限请求、规划模式        │
│  信息灰          245     #8A8A8A    次要信息、状态栏          │
│  边框灰          238     #444444    分隔线、边框              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 色彩应用规则

- **助手消息**：左侧橙色竖线 `│`，内容使用默认终端色
- **用户消息**：蓝色前缀 `>`，内容使用默认终端色
- **工具调用**：灰色图标 `⎿` + 工具名，参数灰色斜体
- **工具结果**：更深的灰色，缩进显示
- **错误**：红色背景或红色文字
- **权限请求**：黄色高亮，需要用户确认

---

## 4. 组件设计

### 4.1 用户消息

```
> 请帮我重构这个函数，使其更符合 Go 的惯用法

  这是多行输入的第二行
  这是第三行
```

**规范**：
- 前缀：`> `（蓝色，粗体）
- 续行：`  `（两空格缩进）
- 消息间：空一行

### 4.2 助手消息

```
│  好的，我来帮你重构这个函数。首先让我分析一下当前代码的结构。
│
│  我发现以下几个可以改进的地方：
│  1. 错误处理不够统一
│  2. 变量命名可以更清晰
│  3. 可以提取一些辅助函数
│
│  让我先读取文件内容，然后给出具体的修改建议。
```

**规范**：
- 左侧边框：`│`（橙色）
- 内容：Markdown 渲染，支持代码块、列表、强调
- 行间距：紧凑，段落间空一行
- 最大宽度：终端宽度 - 4

### 4.3 工具调用

```
  ⎿ read_file path="main.go" offset=1 limit=50
```

**规范**：
- 图标：`⎿`（橙色）
- 工具名：灰色，粗体
- 参数：灰色，等宽字体
- 长参数：截断至 60 字符，显示 `...`

### 4.4 工具结果

```
    ↪ 文件 main.go 共 328 行，显示第 1-50 行
         1	package main
         2
         3	import (
         4	    "fmt"
         5	    "log"
         6	)
         ...
```

**规范**：
- 图标：`↪`（深灰色）
- 缩进：4 空格
- 最大高度：20 行（可展开）
- 代码行号：灰色

### 4.5 权限请求

```
  ⚠ 权限请求: 写入文件 main.go

  是否允许? [y/n]
```

**规范**：
- 图标：`⚠`（黄色）
- 背景：可选黄色半透明背景
- 输入：单字符响应（y/n/a=全部允许）

### 4.6 错误消息

```
  ✗ 读取文件失败: open /path/to/file: no such file or directory
```

**规范**：
- 图标：``（红色）
- 文字：红色
- 自动滚动到可见区域

### 4.7 系统通知

```
  ✓ 已保存会话 (42 条消息)
  ℹ 已切换到规划模式
  ↻ 上下文已压缩 (120 → 15 条消息)
```

**规范**：
- 图标：根据类型选择（✓/ℹ//⚠/✗）
- 文字：灰色
- 显示 3 秒后淡出（可选）

---

## 5. 交互流程

### 5.1 启动流程

```
[启动]
  │
  ├─ 检查 API Key
  │   └─ 未配置 → 显示配置提示 → 退出
  │
  ├─ 加载配置 (~/.taiji-code/config.json)
  │
  ├─ 加载项目记忆 (TAIJI.md)
  │
  ├─ 恢复会话 (--resume)
  │   └─ 显示"已恢复上次会话"
  │
  └─ 显示欢迎界面 → 进入主循环
```

### 5.2 主循环流程

```
[等待输入]
  │
  ├─ 用户输入消息
  │   ─ 显示用户消息块
  │
  ├─ 发送到 LLM
  │   └─ 显示"思考中..."状态
  │
  ├─ 接收响应
  │   │
  │   ├─ 纯文本 → 显示助手消息块
  │   │
  │   └─ 工具调用 → 显示工具调用块
  │       │
  │       ├─ 需要权限 → 显示权限请求
  │       │   ├─ 允许 → 执行工具
  │       │   └─ 拒绝 → 返回错误
  │       │
  │       └─ 执行工具 → 显示工具结果块
  │           │
  │           └─ 继续循环（如果有更多工具调用）
  │
  └─ 显示最终响应 → 回到等待输入
```

### 5.3 命令处理流程

```
[输入 /command]
  │
  ├─ /help     → 显示帮助信息
  ├─ /clear    → 清空屏幕
  ├─ /compact  → 压缩上下文
  ├─ /cost     → 显示用量统计
  ├─ /mode     → 切换工作模式
  ├─ /style    → 切换输出风格
  ├─ /permissions → 切换权限模式
  ├─ /session  → 会话管理
  ├─ /rewind   → 回退到快照
  ├─ /export   → 导出对话
  ├─ /mcp      → MCP 服务器状态
  ├─ /hooks    → 查看 Hooks
  ├─ /skills   → 查看技能
  └─ /doctor   → 诊断检查
```

---

## 6. 动画效果

### 6.1 加载动画

```
思考中...   →  ⠙  →  ⠹  →  ⠸  →  ⠼  →  ⠴  →  ⠦  →    →  ⠇  →    →  ⠋
```

**规范**：
- 字符序列：`⠋⠙⠸⠼⠴⠦⠧⠇⠏`
- 切换间隔：80ms
- 位置：状态栏左侧

### 6.2 流式输出

```
│  好的，我来帮你...[光标]
│  好的，我来帮你分析这个...[光标]
│  好的，我来帮你分析这个函数的结构。[光标]
```

**规范**：
- 逐字显示，无延迟
- 光标位置跟随最新文字
- 完成后移除光标

### 6.3 工具调用展开

```
[折叠]  ⎿ read_file path="main.go"
[展开]   read_file path="main.go"
          ↪ 文件内容...
```

**规范**：
- 默认折叠，显示工具名和参数
- 点击/按键展开显示结果
- 展开动画：向下平滑展开

---

## 7. 响应式设计

### 7.1 终端尺寸适配

| 终端宽度 | 布局调整 |
|----------|----------|
| < 60 列 | 隐藏状态栏右侧提示，工具参数单行显示 |
| 60-100 列 | 标准布局 |
| > 100 列 | 内容区最大宽度 100 列，居中显示 |

### 7.2 内容区高度

```
内容高度 = 终端高度 - 5行（头部+状态栏+分隔线+输入区）
最小高度 = 10行
```

---

## 8. 键盘快捷键

### 8.1 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息 |
| `Shift+Enter` | 换行（多行模式） |
| `Ctrl+C` | 中断当前操作 / 退出 |
| `Ctrl+L` | 清屏 |
| `Ctrl+R` | 搜索历史 |
| `↑/↓` | 历史消息导航 |
| `Tab` | 自动补全（命令/文件路径） |
| `Shift+Tab` | 切换工作模式 |

### 8.2 内容区快捷键

| 快捷键 | 功能 |
|--------|------|
| `PgUp` | 向上滚动 10 行 |
| `PgDn` | 向下滚动 10 行 |
| `Home` | 滚动到顶部 |
| `End` | 滚动到底部 |
| `g` | 滚动到顶部（Vim 模式） |
| `G` | 滚动到底部（Vim 模式） |

### 8.3 Vim 模式额外快捷键

| 快捷键 | 功能 |
|--------|------|
| `j/k` | 上下滚动 |
| `/` | 搜索内容 |
| `n/N` | 下一个/上一个匹配 |
| `v` | 切换 Vim 模式 |

---

## 9. 状态指示器

### 9.1 工作模式

```
[标准]  无额外标识
[规划]  黄色 [规划] 标签
[自动]  绿色 [自动] 标签
```

### 9.2 权限模式

```
[默认]    需要确认危险操作
[自动编辑]  自动允许文件写入
[仅计划]    只读，不执行操作
[绕过]    允许所有操作
```

### 9.3 连接状态

```
● 已连接    (绿色圆点)
○ 未连接    (灰色圆点)
◐ 重连中... (黄色半圆)
```

---

## 10. 完整界面示例

### 10.1 典型对话流程

```
─────────────────────────────────────────────────────────────┐
│  太极 Code                                                    │
─────────────────────────────────────────────────────────────┤
│                                                             │
│  > 帮我检查这个项目的代码质量                                 │
│                                                             │
│  ⎿ list_dir path="."                                        │
│    ↪ 找到 15 个文件和 3 个目录                               │
│                                                             │
│  ⎿ read_file path="main.go" limit=100                       │
│    ↪ 文件 main.go 共 328 行，显示第 1-100 行                 │
│         1	package main                                     │
│         2                                                   │
│         3	import (                                         │
│         ...                                                 │
│                                                             │
│  │  我分析了你的项目，发现以下几个可以改进的地方：            │
│  │                                                          │
│  │  1. **错误处理**：部分函数没有检查 error 返回值           │
│  │  2. **命名规范**：变量名 `x`、`temp` 不够语义化           │
│  │  3. **代码重复**：相似的文件读取逻辑出现了 3 次           │
│  │                                                          │
│  │  要我帮你修复这些问题吗？                                 │
│                                                             │
─────────────────────────────────────────────────────────────┤
│  对话 1 轮    Shift+Tab 切换模式  /help 帮助                 │
├─────────────────────────────────────────────────────────────┤
│  > 是的，请帮我修复                                          │
─────────────────────────────────────────────────────────────┘
```

### 10.2 权限请求界面

```
┌─────────────────────────────────────────────────────────────┐
│  太极 Code                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  > 帮我修复这些代码问题                                       │
│                                                             │
│  ⎿ edit_file path="main.go" old_string="..." new_string=".."│
│                                                             │
│  ⚠ 权限请求: 修改文件 main.go                               │
│                                                             │
│     将第 45-52 行的错误处理改为统一格式                      │
│                                                             │
│  是否允许? [y/n/a(全部允许)]                                 │
│                                                             │
─────────────────────────────────────────────────────────────┤
│  对话 2 轮    [自动]    Shift+Tab 切换模式  /help 帮助       │
├─────────────────────────────────────────────────────────────┤
│  > _                                                        │
└─────────────────────────────────────────────────────────────┘
```

### 10.3 错误处理界面

```
┌─────────────────────────────────────────────────────────────┐
│  太极 Code                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  > 读取 /tmp/test.txt                                       │
│                                                             │
│  ✗ 读取文件失败: open /tmp/test.txt: no such file or directory│
│                                                             │
│  > _                                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 11. 实现优先级

### P0 — 核心界面（必须实现）

- [x] 基础布局（头部/内容/状态栏/输入区）
- [x] 用户消息渲染
- [x] 助手消息渲染（Markdown 支持）
- [x] 工具调用/结果显示
- [x] 输入提示符和光标
- [x] 基础配色方案

### P1 — 增强体验（应该实现）

- [x] 流式输出动画
- [x] 加载动画（⠋⠙⠹...）
- [x] 权限请求界面
- [x] 错误消息样式
- [x] 系统通知样式
- [x] 滚动支持

### P2 — 高级功能（可以实现）

- [ ] 工具结果折叠/展开
- [ ] 历史搜索（Ctrl+R）
- [ ] 自动补全（Tab）
- [ ] Vim 模式完整支持
- [ ] 多行输入模式
- [ ] 内容区宽度限制和居中

### P3 — 锦上添花（可选实现）

- [ ] 消息淡入动画
- [ ] 工具调用进度条
- [ ] 自定义主题支持
- [ ] 界面缩放支持

---

## 12. 与 Claude Code 的差异点

| 特性 | Claude Code | 太极 Code |
|------|-------------|-----------|
| 品牌色 | 橙色 (#D4A574) | 太极橙 (#E06050) |
| 工具图标 | `⎿` | `⎿` (保持一致) |
| 边框样式 | 左侧竖线 | 左侧竖线 (保持一致) |
| 权限提示 | 黄色高亮 | 黄色高亮 (保持一致) |
| 加载动画 | 旋转字符 | 旋转字符 (保持一致) |
| 中文支持 | 有限 | 完整 UTF-8 支持 |
| 本地化 | 英文 | 中英双语 |
| 额外功能 | - | 太极美学元素、中文优化 |

---

*文档版本: 1.0*
*最后更新: 2026-07-28*
